import 'package:flutter/widgets.dart';

class VehicleFuelWidget extends StatelessWidget {
  const VehicleFuelWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
